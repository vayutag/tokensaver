# Production environment configuration
# The deployed backend (Render) origin — baked into the build automatically.
VITE_API_BASE_URL=https://tokensaver-api-yyta.onrender.com
# 50 MB cap to match the free-tier backend instance.
VITE_MAX_FILE_SIZE=52428800
VITE_APP_NAME=TokenSaver
VITE_APP_ENV=production
